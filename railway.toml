[build]
builder = "dockerfile"

[deploy]
startCommand = "sh -c 'gunicorn app:app --bind 0.0.0.0:${PORT:-5000} --workers 2 --timeout 150'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
