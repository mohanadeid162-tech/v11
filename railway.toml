[build]
builder = "nixpacks"

[deploy]
startCommand = "sh -c 'gunicorn app:app --bind 0.0.0.0:${PORT} --workers 2 --timeout 300 --worker-class sync'"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
